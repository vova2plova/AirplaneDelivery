<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:viewmodels="clr-namespace:Frontend.ViewModels" x:DataType="viewmodels:LoginViewModel"
             x:Class="Frontend.Views.LoginPage"
             Shell.NavBarIsVisible="True">
    <ContentPage.Content>
        <StackLayout Padding="10,0,10,0">
            <Label TextTransform="Uppercase" FontSize="24" Text="Вход" Margin="0,100,0,100" HorizontalOptions="Center"/>
            <Editor Placeholder="Логин"/>
            <Entry IsPassword="True" Placeholder="Пароль"/>
            <Button VerticalOptions="Center" Text="Вход" Command="{Binding LoginCommand}"/>
            <Button VerticalOptions="Center" Text="Регистрация" Command="{Binding RedirectRegister}"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>