<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" xmlns:_vm="clr-namespace:FrontEnd.ViewsModels"
             x:Class="FrontEnd.MainPage">
    <ContentPage.BindingContext>
        <_vm:MainPageViewModel x:Name="vm"/>
    </ContentPage.BindingContext>
    <ScrollView >
        <StackLayout BindableLayout.ItemsSource="{Binding Product}">


            <BindableLayout.ItemTemplate>
                <DataTemplate>
                    <StackLayout>
                        <Grid HorizontalOptions="Center">
                            <Image Source="{Binding Images}" WidthRequest="170" HeightRequest="130"
                                            HorizontalOptions="Center" VerticalOptions="Center" />
                        </Grid>
                        <Label Margin="10,0,0,0" Text="{Binding Name}" TextColor="Black" FontSize="24" VerticalOptions="Center"  />
                       
                        <Label Text="&#10;"></Label>
                    </StackLayout>
                </DataTemplate>
            </BindableLayout.ItemTemplate>

        </StackLayout>
    </ScrollView>
</ContentPage>
